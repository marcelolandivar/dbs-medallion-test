resources:
  jobs:
    Job:
      name: Job
      queue:
        enabled: true

       tasks:
         - task_key: script_task
           notebook_task:
             notebook_path: /Workspace/Users/ferndaguzmanguzman@gmail.com/dbs-medallion-test/main.py
       email_notifications:
         on_failure:
           - ferndaguzmanguzman@gmail.com
       trigger:
         # Run this job every day, exactly one day from the last run; see https://docs.databricks.com/api/workspace/jobs/create#trigger
         periodic:
           interval: 1
           unit: DAYS
