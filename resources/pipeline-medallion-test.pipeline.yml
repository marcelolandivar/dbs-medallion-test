resources:
  pipelines:
    medallion_pipeline:
      name: medallion-pipeline
      catalog: ${var.env}_catalog
      target: medallion_schema
      serverless: true
      development: ${var.development}
      continuous: false
      channel: CURRENT
      libraries:
        - file:
            path: ../dlt_pipeline.py
