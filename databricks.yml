# Declarative Automation Bundle configuration, more info here: https://docs.databricks.com/en/dev-tools/bundles/settings.html
bundle:
  name: "Project code"

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  # The 'dev' target, for development purposes. This target is the default.
  dev:
    mode: development
    default: true
    workspace:
      host: https://dbc-6d9d2165-ee4b.cloud.databricks.com
      root_path: /Users/${workspace.user}/.bundle/${bundle.name}/${bundle.target}

  # The 'prod' target, used for production deployment.
  prod:
    mode: production
    workspace:
      host: https://dbc-6d9d2165-ee4b.cloud.databricks.com
      # We always use /Users/ferndaguzmanguzman@gmail.com for all resources to make sure we only have a single copy.
      root_path: /Users/ferndaguzmanguzman@gmail.com/.bundle/${bundle.name}/${bundle.target}
    run_as:
      # This runs as ferndaguzmanguzman@gmail.com in production. We could also use a service principal here,
      # see https://docs.databricks.com/dev-tools/bundles/permissions.html.
      user_name: ferndaguzmanguzman@gmail.com
