# Declarative Automation Bundle configuration, more info here: https://docs.databricks.com/en/dev-tools/bundles/settings.html
bundle:
  name: "Project code"

# Grant permissions to users/service principals who can deploy this bundle
permissions:
  - level: CAN_MANAGE
    user_name: ferndaguzmanguzman@gmail.com
  # Add your GitHub Actions service principal here:
  # - level: CAN_MANAGE
  #   service_principal_name: "<your-service-principal-app-id>"

include:
  - resources/*.yml
  - resources/*/*.yml

  # Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  env:
    description: The environment to use
    default: dev

targets:
  # The 'dev' target, for development purposes. This target is the default.
  dev:
    mode: development
    default: true
    variables:
      env: dev
    workspace:
      host: https://dbc-f68e7a30-2eef.cloud.databricks.com/
      root_path: ~/.bundle/${bundle.name}/${bundle.target}

  # The 'prod' target, used for production deployment.
  prod:
    mode: production
    variables:
      env: prod
    workspace:
      host: https://dbc-f68e7a30-2eef.cloud.databricks.com/
      # We always use /Users/ferndaguzmanguzman@gmail.com for all resources to make sure we only have a single copy.
      root_path: ~/.bundle/${bundle.name}/${bundle.target}
    run_as:
      # This runs as ferndaguzmanguzman@gmail.com in production. We could also use a service principal here,
      # see https://docs.databricks.com/dev-tools/bundles/permissions.html.
      user_name: ferndaguzmanguzman@gmail.com
